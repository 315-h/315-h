# 《经历翻译官》网页 UI 设计审计报告

> 审计对象：`index.html`（单文件主程序，上一轮已完成对比度/离线纯净/交互细节优化后的最新状态）
> 审计维度：布局 · 配色 · 字体排版 · 间距对齐 · 交互反馈 · 可访问性 · 响应式
> 方法：基于源码逐行核查，所有结论附行号依据，避免泛泛而谈。

---

## 一、界面类型与核心技术栈

| 项 | 结论 |
|---|---|
| **界面类型** | 单文件 Web 应用（SPA，纯前端演示载体），**非移动 App、非后台管理系统**。主要场景：桌面浏览器演示 + 作品集落地页。 |
| **技术栈** | 原生 HTML5 + CSS3（CSS 变量 / Grid / Flexbox）+ 原生 JS（无框架、无构建，单文件 ~1833 行）。LocalStorage 持久化。可选调用本地 FastAPI + LangChain 后端（DeepSeek / Qwen）。 |
| **已具备的优良基础** | `lang="zh-CN"`（`L2`）、viewport 声明（`L5`）、`:focus-visible` 焦点环（上轮补）、`prefers-reduced-motion` 降级（`L62`）、表单 `<label>`（配置区 `L603–623`）、图标按钮 `aria-label`（主题键 `L529`）、`@media print` 打印样式（`L502`）、双层 loading（全屏 + 卡片局部）、选中态有明确 ring 反馈。 |
| **待改进总评** | 桌面端完成度高；**暗色模式存在硬编码色隐患、报告卡中段断点缺失、Toast 无语义/无读屏、间距刻度不统一**是四个最该收口的点。 |

---

## 二、分维度诊断（证据 → 具体改法）

### 1. 布局 Layout
- **报告卡动作列断点过晚**（证据 `L490–494`：`@media (max-width:560px)` 才把 `.block-actions` 从纵向 128px 改为横向 wrap）。在 **560–880px 区间**（常见手机横屏 640–800、小平板）仍是「正文 + 右侧 128px 动作列」横向布局，正文被压窄、改写按钮极挤。
  - **改法**：将动作列断点前移到 `720px`——`@media (max-width:720px){ .block-card{flex-direction:column} .block-actions{flex-direction:row;width:100%;flex-wrap:wrap} }`，并保留 `560px` 处 `min-width:calc(50%-4px)` 的两列按钮。
- **首屏 hero 两列撑到 880px 才塌**（证据 `L160` `grid 1.15fr .85fr` + `L477` 断点 880）。600px 手机上插画仍占右半、文字挤压；且 `align-items:center` 使左右高度不齐时留白不均。
  - **改法**：在 `720px` 增加 hero 单列塌缩（`.hero{grid-template-columns:1fr}` + 隐藏/缩小插画），让窄屏纯文字流更稳。
- **报告页 6 大模块长滚动无定位锚点**（证据 `L341` `.block-card` 顺序渲染，无侧边目录/进度）。用户生成后长滚易迷失、不便跳模块。
  - **改法**：报告栏 `.report-bar`（`L318`）内加「模块锚点」胶囊或右侧「回到顶部」浮动按钮；或在 ≥1024px 加 sticky 模块导航。

### 2. 配色 Color
- **暗色模式硬编码文字色隐患（最隐蔽的真问题）**（证据 `L350–352`：`.block-tag.blue` 文字 `#5C83A8`、`.block-tag.green` 文字 `#5E735E` 为硬编码 hex，底为 `rgba(148,184,217,.16)` / `rgba(185,203,185,.22)`）。浅色模式下对比刚好；但暗色 `--card` 变深后，浅蓝/浅绿底叠加变暗，中蓝文字 `#5C83A8` 压在更暗底上 **对比度跌破 AA**。
  - **改法**：把这两个 tag 文字色改为 token，浅/暗各给一值（如 `--tag-blue-text` 浅 `#4A6E8C` / 暗 `#A9C6E0`；`--tag-green-text` 浅 `#4E6B4E` / 暗 `#A8C7A8`），或用 `color-mix` 相对 `--text` 生成。
- **强调色单一**：全站仅珊瑚 `--primary` 一种强调，信息层级靠蓝/绿 tag 区分但蓝绿仅用于标签，主 CTA/选中/链接全珊瑚，长报告里视觉重点略平。
  - **改法（可选）**：把「岗位匹配度」等正向指标统一用绿、「可迁移能力」用蓝描边，形成稳定的"色—模块"映射，帮助用户扫读。

### 3. 字体排版 Typography
- **非标准字重 `font-weight:780`**（证据 `L178` `.hero-text h2{font-weight:780}`）。780 非 100 整数倍，浏览器会回退到 700 或 800，渲染不确定、跨平台不一致。
  - **改法**：改为 `800`（或 `700`）。
- **字号层级compressed**：`section-title` 18px（`L185`）与 `block-title` 16.5px（`L353`）仅差 1.5px，模块标题辨识度弱；`block-title 16.5` 与正文 `14.5` 仅 1.1×。
  - **改法**：拉开阶梯——`section-title` → `20px`、`block-title` → `18px`、正文保持 `14.5px`，形成 20/18/14.5 清晰三级；标题字重统一 `700`。

### 4. 间距与对齐 Spacing / Alignment
- **间距无统一刻度**：散值 `38px`（`L185` `margin:38px 0 16px`）、`22px`（`L94` `padding:12px 24px`→实际 `12px 24px`，但 `L321`/`L327` 用 22/18/14）、`14px`（`L319` gap:14px）均为非 8 倍数，视觉节奏略乱。
  - **改法**：建立 8 倍数刻度（8/16/24/32/40），将 `38→40`、`22→24`、`14→16`（或 12 体系）。至少把同组件内 gap 对齐到同一刻度。
- **录入页控件过窄**（`L245` `select.ctl,input.ctl{max-width:420px}`、`L256` textarea 无 max 但父容器限宽）：桌面大屏（>1024）配置区 + 文本框被限制在 420px，左右大片留白，空间利用率低。
  - **改法**：桌面端改为「左输入（占 1.4fr）/ 右配置卡（占 1fr）」两栏 Grid（`≥860px`），窄屏回退单列；或把 `max-width` 提到 `560px`。

### 5. 交互反馈 Interaction
- **Toast 单例覆盖 + 语义错**（证据 `L1508–1514` `toast()` 直接覆盖 `#toast-msg` 并重置 1.9s 计时器；`L407` 图标永远 `var(--green)`）。后果：① 连续 toast 互相闪掉（如 OCR 失败后再「已删除」被覆盖）；② **错误/警告也显示绿色对勾图标，语义错误**。
  - **改法**：`toast(msg, type)` 增加 `type∈{ok|warn|err}`，映射图标色（绿/橙/红）与边框；用队列或至少 `aria-live`；错误 toast 用红 `#E2574C` 图标。
- **Loading 遮罩 z-index 低于 Toast**（`L301` z-index:90 vs `L403` toast z-index:120）：生成中若弹 toast 会盖在 loading 上，层级混乱。
  - **改法**：loading 遮罩 `z-index` 提到 `200`；且生成期间禁用「生成」按钮防重复提交（防抖）。
- **选中态仅靠颜色+阴影**（证据 `L219` `.job-chip.selected`、`.mode-card.selected` `L195`）：色盲用户难区分选中/未选（无「✓」符号）。
  - **改法**：selected 态加 `::after` 勾选标记或左侧加 `✓`，不只靠颜色。

### 6. 可访问性 Accessibility
- **Toast 无读屏播报**：`#toast`（`L400`）非 `aria-live`，屏幕阅读器用户收不到"已复制/已保存"等反馈。
  - **改法**：`#toast` 加 `role="status" aria-live="polite"`；错误类用 `aria-live="assertive"`。
- **暗色硬编码 tag 文字色**（同配色第 2 点）：属对比度类 a11y 缺陷，需随配色一并修。
- **已达标项（确认无需改）**：`lang`、viewport、表单 `label`、主题键 `aria-label`、`:focus-visible`、reduced-motion 均已就位。

### 7. 响应式 Responsive
- **断点缺口**：仅 `880`（`L474/477`）+ `560`（`L490`）+ `print`（`L502`）。缺 **720px（手机横屏/小平板）** 与 **1024px（大平板）** 中间断点。
  - **改法**：补 `@media (max-width:720px)`（hero 单列、报告卡动作列横向化）与 `@media (max-width:1024px)`（录入页回退单列、报告栏换行对齐优化）。
- **topbar 窄屏未压缩**（`L94` `.topbar .row{padding:12px 24px}`）：360px 屏上「返回 + 品牌 + 主题」三元素间距未收紧。
  - **改法**：`≤480px` 时 `padding:10px 16px`、品牌字号略缩。
- **录入页打印/窄屏**：textarea `max-width` 限制导致窄屏输入区被不必要收窄（证据同布局第 4 点）。

---

## 三、优先级排序（可落地清单）

| 优先级 | 优化点 | 具体落地动作 | 工作量 | 风险 |
|---|---|---|---|---|
| **P0** | Toast 类型化 + 读屏 | `toast(msg,type)` 加 ok/warn/err 配色（绿/橙/红），`#toast` 加 `role="status" aria-live` | 小 | 低 |
| **P0** | 暗色硬编码 tag 文字色 | `.block-tag.blue/.green` 文字改 token（浅/暗双值）或 `color-mix` | 小 | 低 |
| **P0** | 报告卡动作列断点前移 720px | 新增 `@media(max-width:720px)` 改竖向 wrap，消除 560–880 挤压 | 小 | 低 |
| **P1** | 统一间距 8 倍数刻度 | `38→40`/`22→24`/`14→16`，同组件 gap 对齐 | 中 | 低 |
| **P1** | 字号层级拉开 | `section-title 18→20`、`block-title 16.5→18`、标题 `font-weight 780→800` | 小 | 低 |
| **P1** | 录入页桌面两栏布局 | `≥860px` 左输入/右配置 Grid，窄屏单列；放宽 `max-width:420` | 中 | 中 |
| **P1** | 报告页模块锚点/回到顶部 | `.report-bar` 加锚点胶囊或浮动回顶按钮 | 中 | 低 |
| **P2** | 补 720/1024 响应式断点 | hero 单列、topbar 窄屏压缩、录入页回退 | 中 | 低 |
| **P2** | 选中态加勾选符号 | job-chip/mode-card selected 加 `✓`，色盲友好 | 小 | 低 |
| **P2** | Loading z-index 与防重复提交 | 遮罩 z-index→200；生成中禁用按钮 | 小 | 低 |
| **P2** | 强调色—模块映射（可选） | 蓝=能力/绿=匹配度，稳定扫读线索 | 中 | 中 |

---

## 四、建议落地顺序

1. **先收 P0**（约 3 处小改，半天量）：直接消除暗色对比隐患、Toast 语义/读屏、报告卡中段挤压——这三项是"不看代码发现不了、但用户会真实踩到"的硬伤。
2. **再理 P1**：间距刻度 + 字号层级是"整体精致度"的杠杆，改动小、观感提升明显；录入页两栏与报告锚点视大屏演示需求而定。
3. **P2 按需**：响应式补断点、选中勾选、loading 防重，属打磨项，可并入下次迭代。

> 注：本报告仅覆盖 `index.html`。若需把作品集首页 `portfolio.html` 与三详情页一并纳入统一审计（含 token 词表对齐 `--c/--divider` 等），可单独发起。
